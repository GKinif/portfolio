<section class="flex flex-col px-6">
    <h1>Edit Photo</h1>

    <%= render "form.html", Map.merge(assigns, %{action: Routes.admin_photo_path(@conn, :update, @photo), albums: @albums}) %>

    <span><%= link "Back", to: Routes.admin_photo_path(@conn, :index) %></span>

</section>